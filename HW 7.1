import string
fh = open("words.txt")

def forloop(fh):
    words = []
    for line in fh.readlines():
        word = line.split()
        stripped = "".join(w for w in word if w not in string.punctuation)
        words.append(stripped.lower())
    print words

def listcomp(fh):
    words = [i.lower() for i in ("".join(w for w in word if w not in string.punctuation) for word in (line.split() for line in fh.readlines()))]

def gencomp(fh):
    genwords = (i.lower() for i in ("".join(w for w in word if w not in string.punctuation) for word in (line.split() for line in fh.readlines())))

def mapfunc(fh):
    words = map(lambda line: line.split(), fh.readlines())
    print words
mapfunc(fh)
